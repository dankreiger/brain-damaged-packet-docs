import React from 'react';
import { ClientServerContainer } from './ClientServer.styles';

const ClientServer = () => {
  return <ClientServerContainer>hi</ClientServerContainer>;
};

export default ClientServer;
